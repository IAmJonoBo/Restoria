__all__ = [
    "__version__",
]

# The gfpp package is a new, modular runtime that can live alongside the
# existing gfpgan package. It intentionally keeps optional dependencies behind
# lazy imports and provides graceful fallbacks.

__version__ = "0.0.1"

