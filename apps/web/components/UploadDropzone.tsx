import React from "react";

export default function UploadDropzone() {
  return (
    <div role="region" aria-label="Upload" style={{ border: "2px dashed #ccc", padding: 16 }}>
      <p>Drop files here or click to upload (stub).</p>
    </div>
  );
}

