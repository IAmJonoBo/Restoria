# Experimental constraints for Python 3.11+
# Align with Basicsr <= 1.4.2 compatibility. Avoid torchvision >=0.17.

--constraint constraints-3.10-compat.txt  # inherit common pins where reasonable

# Core deep learning stack
torch>=2.0,<3
torchvision>=0.13,<0.17

# Basicsr pinned via inherited constraint (1.4.2)

# Scientific stack compatible with torch 2.x
numpy>=2,<3
scipy>=1.12
scikit-image>=0.22
opencv-python>=4.8

# Keep these aligned with runtime
tensorboard
facexlib>=0.3.0
tqdm
lmdb
PyYAML
