# Release Notes (GFPP)

- 0.0.1 (scaffold)
  - Added new modular runtime `gfpp` under `src/` with pluggable backends and metrics wrappers.
  - Minimal CLI `gfpup run` for GFPGAN baseline with manifest + metrics.
  - API skeleton (FastAPI) with `/restore` sync endpoint.
  - Web UI stubs and bench harness with CSV + HTML template.

