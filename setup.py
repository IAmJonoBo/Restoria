#!/usr/bin/env python
from setuptools import setup

# Minimal shim: all metadata and configuration are in pyproject.toml.
# This allows legacy workflows (python setup.py bdist_wheel) to keep working.

if __name__ == "__main__":
    setup()
